package ro.systematic.workshops.di.testdata.impl;

import ro.systematic.workshops.di.testdata.MockedInjectable;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class MockedInjectableImpl implements MockedInjectable {
    private static final Logger logger = LoggerFactory.getLogger(MockedInjectableImpl.class);
}
